-- 04_seed_config.sql: Placeholder for database-level seed config.
-- Programmatic seed will populate authentication and RLS bypassing objects in scripts/seed.ts.

-- Optional check to confirm schema and policies are applied successfully
SELECT 'Database schema, functions, triggers, and RLS policies successfully initialized.' AS status;
